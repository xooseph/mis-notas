Inicializa el repositorio, lo que hace es crear un directorio, el cual contiene el detalle de implementación, almacena versiones de código, ramas.

Genera un directorio `.git`, incluye configuración e historia.
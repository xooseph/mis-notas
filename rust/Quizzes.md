1. **¿Cuál es la herramienta de la línea de comandos para administrar las versiones de Rust en la compu?**
		rustup

2. **Cada programa ejecutable de Rust debe contener una función con el nombre:**
		main

3. **¿Qué hace el comando `rustc hello.rs` en la línea de comandos?**
		`rustc` genera un binario ejecutable llamado `hello`
		
4. **¿Qué NO hace el comando `cargo run`?**
		Cargo observa los cambios en los archivos y re-ejecuta el binario con los cambios nuevos.
		
---------------------------------
5. ¿Qué describe mejor que una variable `x`es inmutable?
		El valor de `x` no se puede cambiar una vez se haya asignado.

6. ¿Cuál es la palabra clave que va después de `let` que indica que la variable puede ser mutada?
		`mut`

7. ¿Cuál es la diferencia entre usar `let`y `const`para declarar una variable?
		`const` se puede usar en un ámbito global y `let`solo dentro de una función.

8. El número más grande representado por el tipo `i128` es:
		2^127 - 1
		
9. Si `x: u8 = 0`, ¿qué pasará si se realiza la operación `x - 1`?
		Depende del modo de compilación.

10. 
